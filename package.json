{
    "name": "anisongdb",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev:api": "bun run --filter ./apps/api dev",
        "dev:web": "bun run --filter ./apps/web dev",
        "build:api": "bun run --filter ./apps/api build",
        "build:web": "bun run --filter ./apps/web build"
    },
    "devDependencies": {
        "typescript": "^5.7.2",
        "wrangler": "^4.60.0"
    },
    "dependencies": {
        "drizzle-orm": "^0.45.1"
    }
}